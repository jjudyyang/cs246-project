#include <iostream>
#include "board.h"
#include "blank.h"

using namespace std;

int main () {

  string command;

  //Implement command interpreter

  while (cin >> command) {
    if (command == "left" ) {
      
    }
    else if (command == "right") {
 
    }
    else if (command == "down") {

    }
    else if (command == "") {

    }
    else if (command == "") {

    }
    else if (command == "") {

    }
    else if (command == "") {

    }
  }

}
